# Trial Registry

Append-only ledger of every backtest run by the research loop, including
failed variants (doc: `skills/qj-research-loop/SKILL.md`). Never edit or
delete a row — if a result is superseded, strike it through
(`~~like this~~`) and add a new row; the history is the point. `n_trials`
for a strategy family's deflated Sharpe calculation is the count of that
family's rows here — count honestly, including the failed ones.

| Timestamp | Strategy | Family | Stage | IR vs. benchmark | Deflated Sharpe | PBO | Cost-sweep survives | Verdict | n_trials (family) | Lessons |
|---|---|---|---|---|---|---|---|---|---|---|
| 2026-07-20 22:32 | Quality composite | Fundamental quality | REVIEW | **FAIL** — IR -0.26, active return -1.89%/yr, cumulative excess -71pts vs SPY, 2016-01-01→2026-07-14 | BLOCKED — never completed; lake API `read_bars` server defect (zero rows for `end` outside ~last 2-3 weeks of wall-clock time) broke 7/8 walk-forward folds across every BACKTEST attempt; single-fold aggregate (0.81) not reported as a real DSR | N/A — `pbo_trials=0`, no optimizer/tuned params to sweep | **PASS** — Sharpe 0.813→0.799 across 0-20bps, edge not cost-sensitive | **Archive** | 1 | Respectable absolute Sharpe (0.80) and CAGR (13.31%) but decisively failed the mandatory IR-vs-benchmark gate — beta, not alpha, across the full decade. A mandatory-gate failure this clear is sufficient grounds for Archive on its own; the still-blocked walk-forward gate doesn't change the outcome either way, so REVIEW proceeded without waiting further (3rd consecutive BACKTEST-stage run stuck on the same infra defect, plus this run's own environment couldn't execute any code/network calls at all — see loop-log.md). Full detail: `docs/research/strategies/quality-composite.md`. |
| 2026-07-21 07:31 | Low-volatility anomaly | Technical / risk-based | REVIEW | **FAIL** — IR -0.41, active return -4.55%/yr, cumulative excess -152.47pts vs SPY, 2016-01-04→2026-07-20 (more decisive than Quality composite's -0.26) | BLOCKED — never completed; same confirmed lake API `read_bars`/`read_features` recency defect (zero rows for `end`/stale `as_of` outside ~last 2-3 weeks of wall-clock time) would strand nearly every fold of the spec's rolling train=24mo/test=6mo scheme, so a full `WalkForwardEngine` run was skipped rather than reproduce an already-predictable near-total-failure result | N/A — `pbo_trials=0`, no optimizer/tuned params to sweep | **PASS** — Sharpe 0.72→0.64 across 0-20bps, total return 206.67%→171.95%, degrades smoothly, edge not cost-sensitive despite 370.88% ann. turnover | **Improve** | 1 | Mandatory IR gate fails decisively over the full 2016-2026 period (beta, not alpha, same failure mode as Quality composite but worse) — yet real regime evidence (diagnostic, computed from `equity_curve.csv` vs. SPY) shows genuine downside protection in both available crisis windows: COVID crash +3.09pts, 2022 bear +11.74pts vs SPY. This isn't a contradiction: the anomaly works exactly as the risk-based hypothesis predicts (defensive in crises, drags in leverage-fueled bull runs) — the full-period IR fails because 2016-2026 is dominated by a long bull stretch between two crises, not because the underlying mechanism is absent. A specific, concrete variant is worth trying rather than archiving outright: gate exposure to the low-vol quintile by market regime (e.g. an SPY-trend or realized-vol regime signal, same pattern already shipped in `sctr_momentum_regime_gated.py`) instead of running the screen unconditionally — spawned as a new Ready-backlog idea. Branch `worktree-low-vol-anomaly` stays alive, not merged. Full detail: `docs/research/strategies/low-volatility-anomaly.md`. |
| 2026-07-21 12:38 | Regime-gated low-volatility anomaly | Technical / risk-based | REVIEW | **FAIL** — IR -0.41 (numerically unchanged from the ungated trial despite different active-return/tracking-error components), active return -3.54%/yr, cumulative excess -123.41pts vs SPY, 2016-01-01→2026-07-21 | BLOCKED — never completed; same confirmed lake API `read_bars` `end`-date recency defect, 3rd consecutive trial hitting it (`end=2020-01-01`/`2023-06-15`/`2026-07-03` all 0 rows, `end=2026-07-21` returns 5300 rows); a rolling train=24mo/test=6mo walk-forward would fail nearly every fold identically, so skipped rather than reproduce a predictable non-result | N/A — `pbo_trials=0`, no optimizer/tuned params to sweep | **PASS** — Sharpe 0.824→0.780 across 0-20bps, total return 227.92%→205.52%, degrades smoothly; regime-gate on/off turnover doesn't add meaningful cost sensitivity | **Archive** | 2 | The SPY-200d-SMA regime gate (binary, gated 449/2650 days ≈17%) left the mandatory IR gate exactly as failed as the ungated version (-0.41 both), and *eroded* the crisis-window protection it was built to preserve: COVID +1.87pts (vs. ungated +3.09pts), 2022 bear +7.27pts (vs. ungated +11.74pts). The pre-registered gate-lag diagnostic explains why: the trend gate was elevated on only 16/24 COVID days (didn't flip until day 6 of a 24-day crash) vs. 153/196 2022-bear days (flipped by day 13 of a much slower decline) — a 200-day SMA is structurally too slow for fast crashes. More fundamentally, the full-period IR failure isn't a crisis-window problem to begin with: the gate is only active ~17% of the time, so a defense-only-during-elevated-risk mechanism can't address the drag that comes from holding a lagging low-vol tilt during the other 83% of (calm, bull-dominated) days — the actual source of the decade's underperformance. A faster/different regime signal would likely trade crisis-timing precision for the same structural ceiling, not clear the gate. Branch `worktree-regime-gated-low-vol` not merged, left parked (not deleted, unattended run). Full detail: `docs/research/strategies/regime-gated-low-volatility-anomaly.md`. |
| 2026-07-21 19:35 | Post-earnings-announcement drift | Fundamental, event-driven | REVIEW | **FAIL** — IR -0.48, most decisive of any trial so far, active return -2.82%/yr, cumulative excess -111.54pts vs SPY, 2016-01-04→2026-07-20 | BLOCKED — never completed; same confirmed lake API `read_bars` `end`-date recency defect, 4th consecutive trial hitting it (`end=2020-01-01`/`2023-06-15`/`2026-07-03` all 0 rows, `end=2026-07-21` returns 5300 rows); a rolling train=24mo/test=6mo walk-forward would strand nearly every fold identically, so skipped rather than reproduce a predictable non-result | N/A — `pbo_trials=0`, no optimizer/tuned params to sweep | **PASS**, but the steepest decay of any trial — Sharpe 0.819→0.775→0.73→0.641 across 0/5/10/20bps (~22% relative decay vs. every prior trial's <11%), consistent with 702.59% ann. turnover, the highest of any trial | **Archive** | 1 | The most decisive IR-gate failure of any trial in this loop (-0.48), and unlike the low-volatility family's consistent both-crisis protection that motivated Improve, PEAD's regime evidence is mixed and non-compensating: COVID crash -3.56pts (underperformed, ~95% gross-invested throughout with no defensive mechanism), 2022 bear +8.42pts (outperformed). Investigated the low tracking error (5.83% ann., much lower than the low-vol trials') directly from `weights.csv`: despite sparse *new*-entry weeks (median 7/week), the fixed 60-day hold means overlapping cohorts keep the book ~95% gross-invested across ~72-90 names on 99.96% of days — a broad, market-like long book in practice, not the sparse/idle character the entry cadence alone would suggest. Combined with the steepest cost-sweep decay and highest turnover of any trial, and no clear salvageable variant (PEAD's behavioral mechanism has no a-priori crisis-regime prediction to confirm, so the mixed regime result carries little design signal the way low-vol's consistent one did), verdict is Archive rather than Improve. Branch `worktree-pead` not merged, left parked (not deleted, unattended run). Full detail: `docs/research/strategies/post-earnings-announcement-drift.md`. |
